@using Library.Web.Models.ViewModels
@model Library.Web.Models.ViewModels.MembersPageViewModel

@{
    ViewData["Title"] = Model.Title;
}

<div class="container">
    <div class="row">
        <h2>Members</h2>
    </div>
<div class="row mb-2 align-items-center">
    <div class="col">
        <a class="btn btn-sm btn-success" asp-controller="Members" asp-action="Add">Add New</a>
    </div>
</div>
</div>

<table class="table table-hover table-striped">
    <thead>
    <tr>
        <th scope="col">ID</th>
        <th scope="col">Name</th>
    </tr>
    </thead>
    <tbody>
    @foreach (Member member in Model.Members)
    {
        <tr>
            <td>@member.Id</td>
            <td>@member.Name</td>
            <td></td>
        </tr>
    }

    </tbody>
</table>
